# Verification record

Local verification dates: 2026-09-09 and 2026-09-10. Host: macOS Apple Silicon. Rust: 1.98.1. PostgreSQL: isolated local 17.11 database. Frontend: Node 26.7.0 / Yarn 4.4.1. All API/payment fixtures use local mock services; no live funds were moved.

## Passed locally

| Area | Evidence |
| --- | --- |
| API coverage | All 47 snapshot operations map uniquely to commands; HTTP integration exercises paths, authentication schemes, all query parameters, complete bodies, exact int64 values, and empty 202 responses. |
| CLI | 11 unit tests and 21 integration tests. Includes profile precedence, scope mismatch, missing IDs, multiple accounts, confirmation, secret destinations, redaction, cursor/offset behavior, OAuth polling/denial/expiry, concurrent process refresh, logout failures, payment recovery/waiting, and stream interruption. |
| Rust quality | CLI formatting and clippy with warnings denied; auth-service formatting and clippy across all targets/features with warnings denied. |
| Auth-service | 173 library tests plus 202 HTTP integration tests. Ten device-flow tests use actual PostgreSQL migrations and cover atomic redemption, rotating refresh, replay, expiry, independent/global revocation, signing claims, rate limits, and HTTP/OpenAPI behavior. Three HTTP regressions added on September 10 cover invalid/expired refresh tokens returning 401, malformed user IDs returning 400, and internal failures remaining 500. |
| Frontend checks | `yarn run check`: seven tasks passed; Svelte checks report zero errors and zero warnings. Three CLI authorization helper tests passed. |
| Browser flow | Eight Chromium tests passed against the isolated mock auth service: login return code, approval, denial/expiry, cross-origin rejection, native forms without JavaScript, rejected-cookie cleanup, deleted-user recovery, and preserved cookies during service outages. |
| Real local stack | Four Chromium flows passed using the actual auth-service, PostgreSQL, frontend, and development CLI: fresh password login, native TOTP MFA login, denial, and recovery from an old localhost session. The suite verifies matching codes, discovery, profiles, private credentials, rotation, session logout, and continued browser login. Videos and transcripts are generated by `scripts/test-local-demo.py`. |
| Native auth setup | `auth-service/scripts/dev.py run` bootstraps an isolated PostgreSQL cluster, local Ed25519/MFA keys, and native-auth users. Its `smoke` command passed password/MFA, pending authorization, single redemption, discovery, rotation, revocation, and browser refresh. |
| Local auth restart | Three Python checks passed for immediate restart after a closed connection, rejecting a live listener without disrupting it, and preserving unrelated socket errors. A full service/database stop and restart also passed on the default demo ports. |
| UI review | Desktop and mobile approval/result captures inspected. Initial approval form accessibility scan: zero axe violations. Route design review passed after two specific fixes. |
| Credential storage | Native macOS Keychain write/read/status/logout smoke test with a temporary fake key passed; the entry was removed. Tests reject insecure file credentials and native-store failure without plaintext fallback. |
| Packaging | Apple Silicon release archive built, extracted, and smoke-tested. Native CI passed macOS ARM64/Intel and Linux ARM64/x86-64 tests and packaging smoke checks on the initial implementation commit. |
| Service delivery | Draft PRs opened in all three repositories. Initial commits by Stephen DeLorme `<stephen@d.elor.me>` are SSH-signed and verified by GitHub. Auth-service CI passed. Maintained PR branches replace the original exported patches. |

The additional standalone frontend E2E TypeScript check reports nine existing errors in unchanged `global.setup.ts` (otplib API) and billing test files. The new local-stack tests introduce no reported type errors; the repository's required `yarn run check` and both browser suites pass.

## Not yet verified or released

- All three PRs remain drafts, not ready for review or merge. No services have been deployed; deployed revisions are **not recorded**.
- Browser approval, issuance, refresh, and revocation have not been exercised together against deployed staging services. Existing-login and fresh-login/MFA acceptance still require staging account access.
- No live payment or checkout integration has run. These require explicitly supplied organization, environment, and test-network wallet identifiers.
- Live Linux desktop Secret Service behavior remains to be checked; CI validates explicit file storage and native binaries. Windows packaging is not part of this release.
- The GitHub release workflow and generated four-platform Homebrew formula have not been published. The workflow requires staging verification and creates a draft release for review.

Browser login is a release gate. Local tests establish the source behavior; they do not satisfy the deployed end-to-end requirement. Follow the ordered rollout in [integration.md](integration.md), then replace the pending items with deployed revisions and recorded evidence.
